services:
  - type: web
    name: cheating-detection-quiz
    env: python
    buildCommand: ""
    startCommand: "gunicorn main:app"
    envVars:
      - key: FLASK_ENV
        value: production
    autoDeploy: true
